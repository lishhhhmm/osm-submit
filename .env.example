# OpenStreetMap OAuth Configuration
# Copy this file to .env.local and fill in your actual values
# .env.local is already gitignored - your secrets are safe!
# Get these from: https://www.openstreetmap.org/oauth2/applications

# Your OAuth Client ID (REQUIRED)
VITE_OSM_CLIENT_ID=

# Your OAuth Client Secret (OPTIONAL - only needed if "Confidential application" was checked)
# For PKCE flow (recommended for public clients), leave this empty
VITE_OSM_CLIENT_SECRET=
